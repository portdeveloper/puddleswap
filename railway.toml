[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.railway-rebalancer"

[deploy]
startCommand = "bash /app/scripts/railway-rebalance-loop.sh"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
